(add-to-list 'load-path "/s/bach/k/under/millerj/.emacs.d/evil")
(require 'evil)
(evil-mode 1)
(custom-set-variables
  ;(add-to-list 'load-path "/home/jason/.emacs.d/site-lisp/color-theme-6.6.0")
	;(require 'color-theme)
	; (require 'viper)
  ;; custom-set-variables was added by Custom.
  ;; If you edit it by hand, you could mess it up, so be careful.
  ;; Your init file should contain only one such instance.
  ;; If there is more than one, they won't work right.
 )
(custom-set-faces
  ;; custom-set-faces was added by Custom.
  ;; If you edit it by hand, you could mess it up, so be careful.
  ;; Your init file should contain only one such instance.
  ;; If there is more than one, they won't work right.
 )
;(add-to-list 'load-path "/home/jason/.emacs.d/site-lisp/color-theme.el")
; (setq auto-save-default t)
; (global-set-key [kp-subtract] 'undo) ; [Undo]
